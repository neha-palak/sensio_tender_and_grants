# >= not == throughout: a hard == pin fails outright (rather than resolving
# to a close, compatible version) when the exact pinned release has no wheel
# for someone's local Python version/platform -- pandas and psycopg2 are the
# two here that actually ship compiled, version-specific wheels, so this is
# a real risk, not a hypothetical one, on a fresh machine.
Flask>=3.0.3
flask-cors>=5.0.1
pandas>=2.2.3
openpyxl>=3.1.5
# psycopg2-binary bundles its own OpenSSL/libpq, which conflicted with the
# system libraries on Render and segfaulted the gunicorn worker on almost
# every request ("Worker was sent code 139"). Source-built psycopg2 links
# against the system libpq instead -- the psycopg2 project itself recommends
# this for production, binary is meant for local dev only. Still needs
# libpq/pg_config on PATH to build at all -- see executables/run_*.command.
psycopg2>=2.9.10
python-dotenv>=1.0.1
gunicorn>=23.0.0

playwright>=1.55.0
playwright-stealth>=2.0.0

redis>=6.4.0

# semantic.py (both scraper packages) embeds locally via sentence-transformers
# (all-MiniLM-L6-v2) -- was never in either v1 repo's requirements.txt either;
# only surfaced here because CI does a genuinely clean install.
sentence-transformers>=3.0.0

google-genai>=1.47.0
google-auth>=2.50.0

httpx>=0.28.1
httpcore>=1.0.9
anyio>=4.12.1
websockets>=15.0.1

pydantic>=2.13.4
pydantic-core>=2.46.4
annotated-types>=0.7.0
typing-inspection>=0.4.2

cryptography>=49.0.0
cffi>=2.0.0
pycparser>=2.23

pyasn1>=0.6.3
pyasn1-modules>=0.4.2

tenacity>=9.1.2
urllib3>=2.0.0
